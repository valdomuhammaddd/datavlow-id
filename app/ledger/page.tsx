import { HistoricalLedger } from "@/components/ledger/historical-ledger";

/** Spreadsheet ledger — alias of analytics for clear routing. */
export default function LedgerPage() {
  return <HistoricalLedger />;
}
